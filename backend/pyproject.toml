[tool.ruff]

line-length = 88
target-version = "py311"
exclude = ["migrations"]

[tool.ruff.lint]

select = [
    "E",
    "F",
    "I",
    "UP",
]

ignore = []

[tool.ruff.format]

quote-style = "double"
indent-style = "space"